# File Path: .github/workflows/build.yml
# CI/CD: setiap push ke main/master otomatis build APK Debug dan diunggah sebagai Artifact.
# Catatan: workflow ini memakai action gradle/actions/setup-gradle sehingga TIDAK butuh
# berkas biner gradle-wrapper.jar di repo (jar biner tidak bisa ditulis lewat teks/browser).
# Jika Anda sudah punya gradlew + gradle-wrapper.jar sendiri, ganti langkah "Build" di bawah
# dengan: run: ./gradlew assembleDebug
name: Build Absen SSP APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Build Debug APK
        run: gradle assembleDebug --stacktrace

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AbsenSSP-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
